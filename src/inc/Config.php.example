<?php

declare (strict_types = 1);

namespace Bingo;

/**
 * Contains the application configuration constants.
 */
class Config
{
	/**
	 * The base URL for the site
	 */
	const BASE_URL = '';

	/**
	 * MySQL host
	 */
	const DBHOST = 'localhost';

	/**
	 * MySQL user
	 */
	const DBUSER = 'root';

	/**
	 * MySQL user password
	 */
	const DBPASS = '';

	/**
	 * MySQL database name
	 */
	const DBNAME = 'bingo';

	/**
	 * The hostname of the Node server
	 */
	const SERVER_HOST = 'localhost';

	/**
	 * The port of the Node server
	 */
	const SERVER_PORT = 3000;

	/**
	 * Secret for authentication with the Node server
	 */
	const SERVER_SECRET = '';

	/**
	 * True to use HTTPS to communicate with the Node server, false to use HTTP
	 */
	const SERVER_HTTPS = false;

	/**
	 * The Twitch application ID
	 */
	const TWITCH_APP_ID = '';

	/**
	 * The Twitch application secret
	 */
	const TWITCH_APP_SECRET = '';
}
